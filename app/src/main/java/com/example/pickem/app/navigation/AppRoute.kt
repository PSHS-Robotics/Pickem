/*
 * Lists the screen names the app can navigate to.
 * This gives the project one simple place to keep route names instead of hardcoding them in multiple files.
 */
package com.example.pickem.app.navigation

enum class AppRoute {
    LOGIN,
    HOME,
    GAMES,
    HISTORY,
    PROFILE,
    SETTINGS
}
