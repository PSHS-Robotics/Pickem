/*
 * This model represents a user profile stored on the device.
 * It is where user identity and app-specific profile values can live.
 */
package com.example.pickem.data.model

data class UserProfile(val id: String = "")
