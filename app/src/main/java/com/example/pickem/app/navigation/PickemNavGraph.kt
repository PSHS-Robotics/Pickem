/*
 * This will hold the app's navigation setup later.
 * For now it is only a placeholder so the project has a clear home for screen-to-screen navigation.
 */
package com.example.pickem.app.navigation

import androidx.compose.runtime.Composable

@Composable
fun PickemNavGraph() {}
