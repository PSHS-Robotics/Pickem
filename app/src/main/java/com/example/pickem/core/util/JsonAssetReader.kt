/*
 * This will be the shared helper for opening and reading JSON files from the app's assets folder.
 * It keeps basic file-reading code in one place instead of repeating it in every data source.
 */
package com.example.pickem.core.util

object JsonAssetReader
