/*
 * This repository will sit between the UI and local bet-history storage.
 * Its job later will be to organize bet-related reads and writes in one place.
 */
package com.example.pickem.data.repository

class BetRepository
